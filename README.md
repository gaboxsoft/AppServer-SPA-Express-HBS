## Ejecuta un servidor de Nodejs para desplegar una pagina SPA

Despliega una pagina SPA usando los frameworks
    - Express como controlador de rutas y 
    - HBS como motor de vistas

Para instalar, usar:
```
npm install
```

